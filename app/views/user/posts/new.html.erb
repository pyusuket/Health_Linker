<h1>投稿フォーム</h1>
<%= form_with model: @post, url: user_posts_path, method: :post  do |f| %>
  <h4>画像</h4>
  <%= f.file_field :image, accept: "image/*" %>
  <h4>タグ</h4>
  <%= f.text_field :tag %>
  <h4>説明</h4>
  <%= f.text_area :body %>
  <%= f.submit '投稿' %>
<% end %>