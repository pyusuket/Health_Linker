<% flash.each do |type, message| %>
  <div id="flash-<%= type %>" class="<%= type == 'notice' ? 'bg-green-100 border border-green-400 text-green-700' : 'bg-red-100 border border-red-400 text-red-700' %> mt-20 px-2 py-3 rounded relative" role="alert">
    <%= message %>
  </div>
<% end %>